#!/usr/bin/env bash

#ddev-generated
## Description: Refresh the 1Password secrets in .ddev/.env.web.op.local
## Usage: op-secrets
## Example: "ddev op-secrets && ddev restart"

set -euo pipefail

# Every 'ddev start' already does this, without failing the start. This is the
# strict version: to refresh without a restart, or to see why the start-time one
# warned. See .ddev/rdg/op-secrets.sh.
project_root="${DDEV_APPROOT:?must be run through ddev}"
bash "$project_root/.ddev/rdg/op-secrets.sh" "$project_root"
echo "Run 'ddev restart' to apply."
