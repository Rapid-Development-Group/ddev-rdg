{
  "name": "libraries-custom",
  "private": true,
  "scripts": {
    "start": "vite build --watch --config vite.config.mjs",
    "build": "vite build"
  },
  "dependencies": {
    "sass-embedded": "^1.103.1"
  }
}
