name: rdg

ddev_version_constraint: '>= 1.24.8'

project_files:
  - rdg/
  - config.rdg.yaml
  - providers/platform.yaml
  - commands/web/rdg-sync

post_install_actions:
  - |
    #ddev-nodisplay
    for f in rdg/*.sh; do [ -e "$f" ] && chmod +x "$f"; done
    chmod +x commands/web/rdg-sync
